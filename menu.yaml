Sistema:
  Usuarios:
    app: authentication
    model: user
  Grupos:
    app: auth
    model: group
  Permisos:
    app: auth
    model: permission
  Auditoría:
    app: tracing
    model: trace
  Reglas de auditoría:
    app: tracing
    model: rule
